<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="popup_window_bottom_anim">
        <item name="android:windowEnterAnimation">@anim/business_moped_pop_slide_in_bottom</item>
        <!-- 指定显示的动画xml -->
        <item name="android:windowExitAnimation">@anim/business_moped_pop_slide_out_bottom</item>
        <!-- 指定消失的动画xml -->
    </style>

</resources>
